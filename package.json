{
  "name": "torch-football",
  "version": "0.20.0",
  "description": "TORCH — The Football Card Game",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:api": "vercel dev",
    "deploy": "git add -A && git commit -m 'deploy' && git push && vercel --prod",
    "deploy:preview": "vercel"
  },
  "keywords": [
    "football",
    "card-game",
    "daily-puzzle"
  ],
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "jsfxr": "^1.4.0"
  }
}
