node_modules/
dist/
.vite/
.env
*.log
.vercel/
.DS_Store
.fuse_hidden*
.aidesigner/*
!.aidesigner/.gitkeep

# IconScout API preview data (generated locally, not part of the repo)
public/_iconscout-data/

# Claude Code personal / runtime files
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Audio source WAVs — preserved locally for re-encoding, not shipped
archive/audio-sources/
